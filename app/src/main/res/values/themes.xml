<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base theme (no action bar) -->
    <style name="Base.Theme.Globally" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize colors/text styles here -->
        <item name="colorPrimary">@color/primary</item>
        <item name="colorOnPrimary">@color/on_primary</item>
        <item name="colorPrimaryContainer">@color/primary_container</item>
        <item name="colorOnPrimaryContainer">@color/on_primary_container</item>
    </style>

    <!-- Main app theme (inherits from Base.Theme.Globally) -->
    <style name="Theme.Globally" parent="Base.Theme.Globally" />
</resources>